# Object Vision Docker Configuration

# Advantech npm registry (must be reachable during docker build)
NPM_REGISTRY=http://172.22.135.200:55000/

# Port mapping (host:container)
HTTP_PORT=80
HTTPS_PORT=443
